version: '3.4'

services:
  bench.api:
    image: ${DOCKER_REGISTRY-}benchapi
    ports: 
      - 8001:80
      - 8000:443
    build:
      context: .
      dockerfile: BenchAPI/Dockerfile
      
  bench.front:
    image: ${DOCKER_REGISTRY-}benchfront
    ports:
      - 4200:80
    build:
      context: .
      dockerfile: BenchFront/Dockerfile

